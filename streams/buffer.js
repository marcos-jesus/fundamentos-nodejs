// Representação de um espaço na memória do computador,
// usado para transitar dados de uma maneira muito rápido.

// Salvar e Ler na memória de uma maneira muito performática.
// Guarda os dados de uma maneira binária.


const buffer = Buffer.from("Hello")

console.log(buffer.toJSON())